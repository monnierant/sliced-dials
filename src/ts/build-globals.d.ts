// Injected by vite at build time from the manifest in `src/`, so the sources
// stay identical whether the package is built as a system or as a module.
declare const __PACKAGE_ID__: string;
declare const __PACKAGE_KIND__: "system" | "module";
